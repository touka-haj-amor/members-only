members=# create table users (id integer primary key generated always as identity, firstname varchar(255), lastnamme varchar(255), username varchar(255) unique not null, membershipstatus varchar(255));
CREATE TABLE
members=# create table messages (
    id integer primary key generated always as identity, 
    title varchar(255), 
    text text not null, 
    timestamp timestamp default current_timestamp, 
    userid integer not null, 
    constraint fkuser foreign key(userid) references users(id) on delete cascade);
CREATE TABLE
members=#